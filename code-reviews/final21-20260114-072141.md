NOT-COMPLETE.

Remaining issues that break the Iteration 21 “FINAL VERIFICATION” checklist (and a couple of cross-spec consistency hazards):

## 1) **Invalid 32-char IIDs still contain forbidden characters (`o`)**
Crockford Base32 alphabet is declared as `0123456789abcdefghjkmnpqrstvwxyz` (excludes `i l o u`), but at least one **32-character** IID still includes `o`:

- **`spec/00-shared/test-vectors.md`** → *Test Vector 3: Bob's Identity*  
  IID shown: `2fofcybfpmka5vf7ge737exo7crgnxsw` (**contains `o`**)
- **`spec/06-rfcs/RFC-0001-identity-document.md`** → *15.3 Second Identity (Bob)*  
  Same IID appears there too.

This violates the checklist item: “No 32-char strings contain i/l/o/u”.

**Fix:** Regenerate Bob’s IID using the Crockford encoder already defined in the test-vector generator (and update any dependent vectors that use Bob’s IID, e.g., X3DH salt construction if applicable).

---

## 2) **`manifest-schema.md` still contains embedded `signature` fields (contradicts SIGNATURE-file-only rule)**
Your checklist item 6 says: “manifest-schema.md: Uses SIGNATURE file, no embedded signature”.

But **`spec/04-app-runtime/manifest-schema.md`** still includes examples that embed:

- `"signature": { "algorithm": ..., "public_key": ..., "signature": ... }`

Specifically:
- **“Minimal Manifest”** example includes a `signature` object (and also uses a nonconforming `files` shape: it shows `"files": { "main.wasm": "sha256:..." }` instead of `files: { hashes: {...}, total_size }`).
- **“Chat Application Manifest”** example also includes an embedded `signature` object.

This is a direct contradiction of the “SIGNATURE file is REQUIRED / manifest.json does NOT contain a signature field” rule repeated elsewhere.

**Fix:** Remove `signature` from all manifest examples, and ensure examples match the schema (`files.hashes`, `files.total_size`). Keep SIGNATURE details exclusively in `05-ux-packaging/app-distribution.md` (or a dedicated signing section) and reference it.

---

## 3) **SIGNATURE field-name mismatch (`signed_manifest_hash` vs `manifest_hash`)**
Not in your checklist, but it will cause implementation divergence:

- **`spec/05-ux-packaging/app-distribution.md`** defines SIGNATURE file field: `signed_manifest_hash: sha256:<hex...>`
- **`spec/04-app-runtime/manifest-schema.md`** defines `SignatureFile.manifest_hash` and compares against that (no `sha256:` prefix in the interface).

**Fix:** Choose one canonical field name and one canonical encoding form:
- Either `signed_manifest_hash` with value format `sha256:<hex>` everywhere, or
- `manifest_hash` with raw hex everywhere,
but don’t mix.

---

## 4) **Mailbox endpoint schema inconsistency (`url` vs `host/port`)**
Also not on your checklist, but it’s cross-layer breaking:

- **Canonical endpoint schema** in **`spec/02-identity-trust/identity-document-schema.md`** uses:
  `type/host/port/transport/priority`
- Mailbox examples in:
  - **`spec/00-shared/layer-integration.md`** and
  - **`spec/06-rfcs/RFC-0003-messaging.md`**
  show mailbox endpoints as `{ "type": "mailbox", "url": "https://..." }`

**Fix:** Pick one canonical representation for mailbox endpoints in the identity document (preferably the shared `Endpoint` schema: `host + port + transport: "https"`), and update RFC-0003 + layer-integration accordingly (or explicitly version/alias the schema, but right now it reads as one shared schema).

---

## 5) `device_transport_key` mention is not fully removed “from all files”
Your checklist item 5 says device_transport_key removed from all files. While it **is** removed from the Device Document schema (good), there are still references in **`spec/05-ux-packaging/node-daemon.md`** (device key hierarchy + `getDeviceTransportKey` etc.). That may be fine as an internal implementation detail, but it contradicts the literal checklist claim and can confuse implementers about whether it is part of v1.

**Fix options:**
- Either delete/omit device-transport-key from daemon spec for v1, or
- Clarify it is *internal-only / reserved*, and explicitly state it is **not published** and **not used by v1 handshake**.

---

## Checklist items that appear OK
- RFC-0002 stream payload typing shows 0x03/0x04 as Binary (looks fixed).
- Test vector generator uses a Crockford alphabet (looks correct).
- Mailbox stores raw PUSE envelopes (layer-integration and RFC-0003 store as octet-stream; retrieval base64 is just API encoding).
- Device Document has no transport-key field (schema is clean).

If you want, I can provide a precise patch plan (by file/section) to get to SPEC-COMPLETE in one sweep.
