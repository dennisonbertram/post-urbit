STATUS: NOT-COMPLETE  
B1: Device Document on-wire schema is internally inconsistent across specs (identity-document-schema.md/RFC-0001 vs 00-shared/layer-integration.md vs 01-transport-connectivity/interfaces.md): `endpoints` is required for device discovery in layer-integration flow but absent elsewhere; field naming alternates between snake_case and camelCase without a normative mapping for device docs, making interoperable parsing/serialization ambiguous.  
B2: Device Document and Device Index DHT record signature schemes are not defined (what exact bytes are signed, required canonicalization, domain separator), yet multiple sections require DHT nodes/peers to verify them; additionally, 00-shared/layer-integration.md “Device Document Verification” references a `signature` field that is not present (should be `signature_by_identity`), creating a concrete verification ambiguity.  
B3: Revocation wire formats are inconsistent and cryptographically underspecified: field names differ between 02-identity-trust/revocation.md and RFC-0001 (e.g., `signatures.by_current_signing_key`/`by_new_signing_key` vs `by_revoked_key`/`by_new_key`), and signature construction/canonicalization/domain separation for `key_revocation`, `identity_revocation`, and `device_revocation` documents (and the DHT revocation record verification) is not defined, preventing correct verification and DHT propagation.  
B4: Sync protocol signing/verification is not implementable as written: `operation_id` and `SyncOperation.signature` depend on `timestamp_bytes` and `operation_bytes` encodings that are not normatively specified (canonical CBOR rules/byte layout for HLC and CRDT ops), and the Merkle-tree hashing/leaf encoding used for efficient sync is not defined sufficiently to guarantee interoperability.
