STATUS: NOT-COMPLETE  
B1: `03-messaging-sync/sync-protocol.md` has conflicting, underspecified canonical encodings for `document_id` and `operation_id` that make signature verification and Merkle construction non-interoperable (e.g., `document_id` stated as “32 bytes (UUID as bytes)” despite UUID being 16 bytes; `operation_id` defined as `hex(SHA256(...))` but later treated as “32 bytes raw SHA256” in `signature_input`). A single unambiguous canonical representation (bytes vs hex/string; fixed vs tagged length) is required.  
B2: `spec/01-transport-connectivity/interfaces.md` defines `interface Endpoint` twice with different field sets/types (one for device discovery, one “canonical schema shared between Identity and Transport”), creating an internal inconsistency and ambiguity about which schema `PeerEndpoints.endpoints`, `DeviceDocument.endpoints`, and `Connection.peerDocument` should use.  
B3: `06-rfcs/RFC-0003-messaging.md` Mailbox authentication token includes `mailbox_url` in the signed material but does not specify a canonical construction/normalization for this URL (scheme/host/port normalization, trailing slash, default port elision, etc.), so independent implementations can generate/verify different signatures for the “same” mailbox endpoint.  
B4: `00-shared/layer-integration.md` “DHT Key Encoding (Normative)” is internally inconsistent about whether DHT keys are SHA256 32-byte hashes or “32-character Crockford Base32 strings”; it must unambiguously specify the key type passed to the DHT API (raw 32 bytes vs encoded string) and clarify that Base32 applies only to the identifier input to the SHA256 derivation.
