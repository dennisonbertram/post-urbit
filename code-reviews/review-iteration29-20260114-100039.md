STATUS: NOT-COMPLETE  
B1: [spec/04-app-runtime/manifest-schema.md:Signature Verification] Internal inconsistency: signing/verification text says `manifest.json` MUST be JCS-canonicalized before hashing, but the provided `verifyPackageSignature()` example hashes raw `manifestBytes` (`sha256(manifestBytes)`), which will reject valid packages (or lead to divergent implementations).  
B2: [spec/00-shared/layer-integration.md:Domain Separator Registry] Internal inconsistency in cryptographic constants: multiple domain separators have incorrect stated byte lengths vs the literal strings shown (e.g., `post-urbit:device-doc:v1:` annotated as 24 bytes though the literal is 25). This can cause implementers to truncate/omit the trailing `:` and produce non-interoperable signatures (security/interop break).  
B3: [spec/01-transport-connectivity/peer-handshake.md:Test Scenarios vs Anonymous Connections] Contradiction: document states anonymous connections are out-of-scope and “All peer-to-peer connections MUST be mutually authenticated”, but test scenario #5 says “Anonymous client: Server accepts anonymous connection”, which could lead to implementing an insecure/undefined mode.
