STATUS: NOT-COMPLETE  
B1: [spec/00-shared/layer-integration.md:Device Document Structure (canonical)] Device document `endpoints` entries omit the required `priority` field, but the canonical Endpoint schema in Identity/Transport (`spec/02-identity-trust/identity-document-schema.md:Endpoint Object` and `spec/01-transport-connectivity/interfaces.md:Endpoint`) makes `priority` REQUIRED. This creates a wire-format/signature interoperability break: implementations will disagree on whether signed device docs must include `priority`, and signature verification/JCS canonicalization will fail across implementations.  
B2: [spec/06-rfcs/RFC-0001-identity-document.md:§15.5 DHT Key Derivation] The example “Identity DHT key input” string is `post-urbit:identity:<iid>` (missing the trailing colon after `identity`), while the normative derivation elsewhere requires `SHA256("post-urbit:identity:" || iid)` (with trailing colon) (e.g., `spec/00-shared/layer-integration.md:DHT Key Encoding`, `spec/06-rfcs/RFC-0001-identity-document.md:§12.1`). This would cause implementations following the RFC example to compute different DHT keys and fail identity lookup (availability failure / partition).
