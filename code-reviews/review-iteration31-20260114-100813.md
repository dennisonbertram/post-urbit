STATUS: NOT-COMPLETE
B1: [spec/03-messaging-sync/sync-protocol.md:Sync Wire Protocol] Sync stream payload is specified as `1-byte Message Type || CBOR Data`, but `spec/00-shared/layer-integration.md` and `spec/06-rfcs/RFC-0002-transport.md §6.3` define stream framing as `4-byte length || payload` with Sync payload described as “CBOR-encoded sync operation/message” (no extra leading type byte). This framing mismatch will cause interoperability failure.
B2: [spec/02-identity-trust/recovery-mechanisms.md:Recovery Status Semantics (Normative) vs Verification pseudocode] The document states `recovery_proof.status` is informational-only and verifiers MUST compute pending/active solely from `now() >= cooldown_expires_at`, but the provided `verify_social_recovery()` logic enforces cooldown only when `status == "pending"`. This internal contradiction enables a bypass if an attacker sets `status="active"` before cooldown, creating a security vulnerability.
B3: [spec/01-transport-connectivity/nat-traversal.md:Candidate Collection / Relay candidate example] The relay candidate collection pseudocode uses `allocation.address` and `allocation.port`, implying a relay-assigned address/port per allocation. This contradicts `spec/01-transport-connectivity/interfaces.md` (`RelayAllocation` has `boundAddress/boundPort` and explicitly *no* relay-assigned port) and the stable relay port model in `relay-protocol.md`/RFC-0002. Implementers cannot derive a correct, consistent relay candidate format from this.
B4: [spec/02-identity-trust/recovery-mechanisms.md:Cooldown and Contestation] The spec defines contestation as cancelling a pending recovery, but does not specify an implementable wire format and distribution mechanism: `contested_sequence` type conflicts with the global “sequence is decimal string” rule, no domain separator is defined for contest signatures, and no transport/DHT publishing key/value format is specified. This leaves contestation behavior undefined and weakens recovery security guarantees.
