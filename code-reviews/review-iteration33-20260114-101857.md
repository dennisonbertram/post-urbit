STATUS: NOT-COMPLETE  
B1: [spec/00-shared/layer-integration.md:Domain Separator Registry] The registry is marked “Normative” and says implementations MUST use exact byte sequences, but it contains incorrect/conflicting byte counts for multiple entries (e.g., `post-urbit:device-doc:v1:` shown elsewhere as “24 bytes” vs registry “25”, and `post-urbit:devices-for:` registry “22” but literal string length is 23). This can lead to implementers truncating/altering separators to “match” the stated byte count, breaking signature verification/interoperability.  
B2: [spec/03-messaging-sync/sync-protocol.md:Operation Signature] The spec states `signature_input` uses domain separator `"post-urbit:sync-op:v1:"` and labels it “21 bytes”, but the literal is 22 bytes and conflicts with `layer-integration.md` (registry says 22). This creates ambiguity for a “must match exact bytes” signing input and can cause cross-implementation signature failures.  
B3: [spec/02-identity-trust/interfaces.md:RecoveryConfig] `RecoveryConfig.config` references `SocialRecoveryConfig | DeviceEscrowConfig | ThresholdConfig | ProviderConfig` but these config types are not defined anywhere in this file (despite claiming “complete API surface”). This leaves the API contract for recovery configuration structurally undefined, forcing implementers to guess/mine other docs.
