STATUS: BLOCKING_ISSUES_FOUND

B1: PUSE test vectors referenced as authoritative do not exist / numbering mismatch
- File(s): 
  - `spec/00-shared/test-vectors.md`
  - `spec/03-messaging-sync/secure-envelope.md`
  - `spec/06-rfcs/RFC-0003-messaging.md`
- Issue: Multiple documents claim that `spec/00-shared/test-vectors.md` contains concrete PUSE envelope construction bytes (and RFC-0003 explicitly references “Test Vector 3 (PUSE Envelope)”). In the current `test-vectors.md`, “Test Vector 3” is Bob’s identity, and there is **no** PUSE envelope vector (no full envelope bytes, no expected parsed fields, no ciphertext/tag/signature values). This is a direct contradiction and prevents implementers from validating PUSE encoding/decoding against the spec’s stated authoritative vectors.
- Fix: Add an explicit PUSE envelope test vector (full byte sequence in hex/base64, including header extension, nonce, ciphertext, signature) to `spec/00-shared/test-vectors.md` and update all references to the correct vector number/name (or renumber test vectors to match references). Ensure the vector includes expected parsed fields (sender/recipient raw IIDs, message_id, ext type/contents, ciphertext_len) and signature verification result.

B2: Device DID test vector appears non-reproducible / inconsistent with the stated seed-based derivation method
- File(s): `spec/00-shared/test-vectors.md`
- Issue: The document states *all* vectors are deterministically derived from the fixed SEED via `derive_key_material(...)`, but “Test Vector 8: Device Identifier (DID) Derivation” contains a device Ed25519 public key hex that looks non-derived (patterned) and a SHA-256 “first 20 bytes” value that appears synthetic. This undermines the “concrete, reproducible” claim and will likely fail cross-implementation verification.
- Fix: Recompute Test Vector 8 from the declared seed derivation method and publish the correct:
  - device signing seed (hex),
  - device signing public key (hex/base64),
  - SHA-256 hash (full or at least enough to verify),
  - truncated 20-byte value (hex),
  - resulting DID (base32).
  Also extend the “Reference Generator” snippet (or add a small section) to generate/print the device DID vectors to guarantee reproducibility.

Non-blocking suggestions (optional):
- In `spec/03-messaging-sync/secure-envelope.md` and `spec/06-rfcs/RFC-0003-messaging.md`, the “Minimum size … (21 = … Ephemeral type)” label is misleading: 21 bytes corresponds to the Group extension (0x02), while the Initial/Ephemeral extension (0x00) is 33 bytes. Consider rewording to “minimum header extension (group type)” or clarify it is the minimum across extension types.
