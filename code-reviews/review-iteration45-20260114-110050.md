STATUS: BLOCKING_ISSUES_FOUND

B1: `spec/03-messaging-sync/group-messaging.md` — **PUSE plaintext `id` field contradicts Secure Envelope + RFC-0003 plaintext schema**
- **Problem:** Group message plaintext example includes `"id": "<message-uuid>"`, but `spec/03-messaging-sync/secure-envelope.md` explicitly states `message_id` is in the envelope header and plaintext “does NOT include a duplicate `id` field.” RFC-0003 §8.1 likewise omits `id`.
- **Fix (exact):** In `spec/03-messaging-sync/group-messaging.md`, in the “Message Content” JSON example, remove the `id` field:
  - Replace:
    ```json
    {
      "type": "text",
      "id": "<message-uuid>",
      "timestamp": "2025-01-13T12:00:00Z",
      "sequence": "42",
      ...
    }
    ```
  - With:
    ```json
    {
      "type": "text",
      "timestamp": "2025-01-13T12:00:00Z",
      "sequence": "42",
      ...
    }
    ```

B2: `spec/06-rfcs/RFC-0002-transport.md` — **Handshake domain separator hex includes illegal NUL/space; contradicts stated 23-byte ASCII string**
- **Problem:** In §11.2 “Challenge Signature”, the DOMAIN hex is shown ending with `...763120` (0x20 = space) and elsewhere `...763100` (0x00 = NUL). This contradicts the normative domain separator `post-urbit-handshake-v1` (ASCII, no NUL terminator) and its stated length (23 bytes).
- **Fix (exact):**
  1) Replace the DOMAIN hex line:
     - Replace:
       ```
       DOMAIN (23 bytes) = 706f73742d757262...763120  // "post-urbit-handshake-v1"
       ```
     - With the full, correct 23-byte hex for ASCII `post-urbit-handshake-v1`:
       ```
       DOMAIN (23 bytes) = 706f73742d75726269742d68616e647368616b652d7631  // "post-urbit-handshake-v1"
       ```
  2) In the `challenge_data (hex) =` block, replace the domain prefix bytes:
     - Replace the first 24 bytes currently shown as:
       ```
       706f73742d757262 69742d68616e6473 68616b652d763100
       ```
     - With:
       ```
       706f73742d757262 69742d68616e6473 68616b652d7631
       ```
     (i.e., remove the trailing `00` and ensure the last byte is `31` for ASCII `'1'`).

B3: `spec/06-rfcs/RFC-0001-identity-document.md` — **IDOC envelope “complete” header length is self-contradictory / not computed**
- **Problem:** §15.6 says “Complete IDOC envelope for genesis document (hex)” and hardcodes length `000001e3` (=483), but immediately notes “Exact length and bytes depend on JCS output.” This is a contradiction: either it’s a concrete test vector (must be exact) or it’s illustrative (must not present a specific length as authoritative).
- **Fix (exact):** Make it explicitly non-test-vector and remove the numeric length claim:
  - Change the heading to “Example IDOC envelope header (illustrative)” and replace:
    ```
    49444f43 01 000001e3
    ...
    000001e3 = length 483 (big-endian uint32)
    ```
  - With:
    ```
    49444f43 01 <length-u32-be>
    ...
    <length-u32-be> = uint32 length of the following UTF-8 JCS JSON
    ```
  - OR (alternative acceptable fix): provide the full canonical JSON bytes and the full envelope hex so that `483` is verifiably correct.

B4: `spec/01-transport-connectivity/quic-integration.md` — **Application error code list contradicts RFC-0002 normative registry**
- **Problem:** The “Application Error Codes (0x100+)” table lists only `0x100`–`0x104`, but RFC-0002 §9.2 defines additional normative codes including `0x105 DUPLICATE_CONNECTION`, `0x106 REVOKED_IDENTITY`, `0x107 REVOKED_KEY`. This creates conflicting/partial registries across docs.
- **Fix (exact):** Update `spec/01-transport-connectivity/quic-integration.md` error table to match RFC-0002 §9.2 by adding:
  - `0x105` DUPLICATE_CONNECTION
  - `0x106` REVOKED_IDENTITY
  - `0x107` REVOKED_KEY
  - (and note `0x108-0x1FF Reserved` if you want it to be explicitly aligned).
