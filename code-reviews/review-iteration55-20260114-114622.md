STATUS: BLOCKING_ISSUES_FOUND

B1: spec/03-messaging-sync/group-messaging.md — **GroupStateUpdate `version` format is self-contradictory**
- **Problem:** The document defines `version` as `"<logical_clock>.<actor_iid_hash>"` and then immediately defines it as `"<logical_clock>.<first_8_chars_of_actor_iid>"`. These are not the same and lead to divergent ordering/collision behavior.
- **Exact fix:** Pick ONE suffix definition and use it consistently. Suggested minimal fix (matches the later text):
  - Change the type comment line:
    - From: `version: string;                 // HLC-style: "<logical_clock>.<actor_iid_hash>" (decimal string)`
    - To:   `version: string;                 // HLC-style: "<logical_clock>.<first_8_chars_of_actor_iid>" (decimal string)`
  - Keep (or update) the later line to match exactly:
    - Ensure it reads: `Full version string: "<logical_clock>.<first_8_chars_of_actor_iid>"`

B2: spec/03-messaging-sync/group-messaging.md vs spec/03-messaging-sync/interfaces.md — **Group role enum mismatch**
- **Problem:** `group-messaging.md` membership examples and role strings use `admin|moderator|member`, while `interfaces.md` defines `type GroupRole = 'owner' | 'admin' | 'moderator' | 'member';`. This is a wire-level authorization primitive; inconsistent role sets are blocking.
- **Exact fix (recommended):** Include `owner` everywhere (since interfaces already include it).
  - In `spec/03-messaging-sync/group-messaging.md`:
    - Update membership schema example:
      - From: `"role": "admin|moderator|member"`
      - To:   `"role": "owner|admin|moderator|member"`
    - Update the authorization table to account for `owner` (or explicitly state `owner` is treated as `admin` for authorization checks).
  - Alternatively (if `owner` is not desired), remove `owner` from `spec/03-messaging-sync/interfaces.md` and adjust any dependent text accordingly (larger change).

B3: spec/04-app-runtime/api-surface.md vs spec/03-messaging-sync/group-messaging.md and spec/03-messaging-sync/interfaces.md — **`join_rule` enum mismatch (`link` missing in Host API)**
- **Problem:** `group-messaging.md` and `03-messaging-sync/interfaces.md` include `join_rule/joinRule` values `invite_only|link|open`, but `api-surface.md` defines `join_rule: 'invite_only' | 'open'` (missing `link`). This breaks cross-layer compatibility for group creation/config.
- **Exact fix:** Update Host API schema to include `link`.
  - In `spec/04-app-runtime/api-surface.md`, under `interface GroupSettings` for `messaging.create_group`:
    - From: `join_rule: 'invite_only' | 'open';`
    - To:   `join_rule: 'invite_only' | 'link' | 'open';`
